To use this verilog, you'll need an fpu in a subdirectory lib/.

You can use the snapshot:

  ln -s fpu_snapshot lib/

...or you can bring in the latest from the repo:

  git clone https://github.com/steveri/fpu lib
